
minimize-on-close = true

"""
the windows last saved state
"""
[saved-state]
; the width of the windows when closed
window-width = 400 ; the width in pixel
window-height = 300 # the height of the windows when closed
windows-x-pos = 20 // the x position when app is closed
windows-y-pos = 40 // the y position when app is closed